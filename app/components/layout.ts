// components/layout.ts
// Un único contenedor fluido para TODA la web (header, hero, services, etc.)
export const CONTAINER =
    "mx-auto w-full px-4 sm:px-6 lg:px-8 " +
    "max-w-[min(100%,90rem)] 2xl:max-w-[min(100%,100rem)]";
